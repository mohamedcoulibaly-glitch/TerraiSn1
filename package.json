{
  "name": "terrainsn-dev",
  "private": true,
  "version": "1.0.0",
  "description": "TerrainSN — commandes de développement unifiées (backend + frontends)",
  "scripts": {
    "dev": "node dev.js",
    "dev:app": "node dev.js --no-admin",
    "dev:admin": "node dev.js --no-frontend",
    "accounts": "node dev.js --accounts",
    "start": "node backend/index.js",
    "build:render": "node scripts/render-build.js",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:unit": "npm run test:unit --prefix backend && npm test --prefix frontend",
    "test:parcours": "npm run test:parcours --prefix backend && npm test --prefix frontend -- parcoursJoueurGerant",
    "test:integration": "npm run test:integration --prefix backend",
    "test:e2e": "npx playwright test",
    "test:e2e:ui": "npx playwright test --ui",
    "test:all": "npm test && npm run test:e2e",
    "lint": "npm run lint --prefix frontend",
    "build": "npm run build --prefix frontend && npm run build --prefix admin-frontend",
    "ci": "npm test && npm run lint && npm run build",
    "docker:build": "docker build -t terrainsn .",
    "docker:up": "docker compose up --build"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  },
  "engines": {
    "node": ">=18"
  }
}
